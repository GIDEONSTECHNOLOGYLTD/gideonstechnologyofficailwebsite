<!DOCTYPE html>
<html>
<head>
    <title>Gideon's Technology - Welcome</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <h1>Welcome to Gideon's Technology</h1>
    <p>Twig is working correctly!</p>
    
    {% if flash.getMessage('success') %}
        <div class="alert alert-success">
            {{ flash.getMessage('success') | first }}
        </div>
    {% endif %}
    
    <nav>
        <ul>
            <li><a href="{{ path_for('home') }}">Home</a></li>
            <li><a href="{{ path_for('products') }}">Products</a></li>
            <li><a href="{{ path_for('cart') }}">Cart</a></li>
        </ul>
    </nav>
    
    <footer>
        <p>&copy; {{ "now"|date("Y") }} Gideon's Technology. All rights reserved.</p>
    </footer>
</body>
</html>
